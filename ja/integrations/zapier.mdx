---
title: Zapier
description: Zapierのウェブフックを通じてDodo Paymentsを5000以上のアプリに接続し、無限の自動化の可能性を実現します。
icon: bolt
---

## はじめに

Zapierを通じてDodo Paymentsを数千のアプリやサービスに接続します。支払いイベントが発生したときにZapsをトリガーすることで、ワークフローを自動化し、メールの送信からスプレッドシートの更新、タスクの作成など、さまざまな作業を行うことができます。

<Info>
この統合には、Zap設定からのZapierウェブフックURLが必要です。
</Info>

## 始め方

<Steps>
<Step title="ウェブフックセクションを開く">
  Dodo Paymentsダッシュボードで、<b>Webhooks → + Add Endpoint</b>に移動し、統合のドロップダウンを展開します。
  <Frame>
    <img src="/images/integrations/zapier.png" alt="エンドポイントの追加と統合のドロップダウン" />
  </Frame>
</Step>

<Step title="Zapierを選択">
  <b>Zapier</b>統合カードを選択します。
</Step>

<Step title="ZapierでZapを作成">
  Zapierで「Webhooks by Zapier」をトリガーとして新しいZapを作成します。ウェブフックURLをコピーします。
</Step>

<Step title="ウェブフックURLを貼り付ける">
  ZapierのウェブフックURLをエンドポイント設定に貼り付けます。
</Step>

<Step title="変換を設定">
  変換コードを編集して、Zapierワークフロー用にデータをフォーマットします。
</Step>

<Step title="テストと作成">
  サンプルペイロードでテストし、<b>Create</b>をクリックして統合を有効にします。
</Step>

<Step title="完了！">
  🎉 支払いイベントが自動的にZapierワークフローをトリガーします。
</Step>
</Steps>

## 変換コードの例

### 基本的なウェブフックペイロード

```javascript basic_webhook.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const p = webhook.payload.data;
    webhook.payload = {
      event_type: webhook.eventType,
      payment_id: p.payment_id,
      amount: (p.total_amount / 100).toFixed(2),
      currency: p.currency || "USD",
      customer_email: p.customer.email,
      customer_name: p.customer.name,
      payment_method: p.payment_method || "unknown",
      timestamp: webhook.payload.timestamp
    };
  }
  return webhook;
}
```

### サブスクリプションイベントハンドラー

```javascript subscription_webhook.js icon="js" expandable
function handler(webhook) {
  const s = webhook.payload.data;
  switch (webhook.eventType) {
    case "subscription.active":
      webhook.payload = {
        event_type: "subscription_started",
        subscription_id: s.subscription_id,
        customer_email: s.customer.email,
        customer_name: s.customer.name,
        product_id: s.product_id,
        amount: (s.recurring_pre_tax_amount / 100).toFixed(2),
        frequency: s.payment_frequency_interval,
        next_billing: s.next_billing_date,
        timestamp: webhook.payload.timestamp
      };
      break;
    case "subscription.cancelled":
      webhook.payload = {
        event_type: "subscription_cancelled",
        subscription_id: s.subscription_id,
        customer_email: s.customer.email,
        cancelled_at: s.cancelled_at,
        cancel_at_next_billing: s.cancel_at_next_billing_date,
        timestamp: webhook.payload.timestamp
      };
      break;
  }
  return webhook;
}
```

### 争議アラートハンドラー

```javascript dispute_webhook.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType.startsWith("dispute.")) {
    const d = webhook.payload.data;
    webhook.payload = {
      event_type: webhook.eventType,
      dispute_id: d.dispute_id,
      payment_id: d.payment_id,
      amount: (d.amount / 100).toFixed(2),
      status: d.dispute_status,
      stage: d.dispute_stage,
      remarks: d.remarks || "",
      urgent: webhook.eventType === "dispute.opened",
      timestamp: webhook.payload.timestamp
    };
  }
  return webhook;
}
```

## 人気のZapierユースケース

<AccordionGroup>
<Accordion title="メール通知">
- 支払い確認のためにGmail/Outlookメールを送信
- Mailchimp/ConvertKitでメールシーケンスを作成
- Slack/Discord通知を送信
- Google Sheetsにレコードを作成
</Accordion>

<Accordion title="CRMの更新">
- HubSpot/Salesforceに連絡先を追加
- Pipedrive/Closeで取引を作成
- Airtableで顧客レコードを更新
- Monday.comで活動を記録
</Accordion>

<Accordion title="タスク管理">
- Asana/Trelloでタスクを作成
- NotionでTo-Doアイテムを追加
- カレンダーイベントを作成
- Twilioを介してSMS通知を送信
</Accordion>
</AccordionGroup>

## ヒント

- Zapierの解析を容易にするためにペイロード構造をシンプルに保つ
- すべてのイベントで一貫したフィールド名を使用する
- ワークフローのタイミングのためにタイムスタンプを含める
- 本番環境に移行する前にサンプルデータでZapをテストする
- 条件付きロジックのためにZapierの組み込みフィルターを使用する

## トラブルシューティング

<AccordionGroup>
<Accordion title="Zapがトリガーされない">
- ウェブフックURLが正しくアクティブであることを確認する
- ZapierでZapがオンになっていることを確認する
- ペイロード構造がZapierの期待に一致していることを確認する
- Zapierダッシュボードでウェブフックの配信をテストする
</Accordion>

<Accordion title="データが正しくマッピングされない">
- Zapierアクションステップのフィールド名を確認する
- データ型が期待されるフォーマットと一致していることを確認する
- Zapierのテスト機能を使用してマッピングをデバッグする
- 変換コードが有効なJSONを返すことを確認する
</Accordion>
</AccordionGroup>