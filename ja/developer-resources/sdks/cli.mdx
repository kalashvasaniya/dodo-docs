---
title: CLI
description: ターミナルからDodo Payments APIと対話するためのコマンドラインインターフェース
icon: terminal
---

## インストール

### Goインストール

```bash
go install github.com/dodopayments/dodopayments-cli/cmd/dodopayments@latest
```

### ソースから

```bash
git clone https://github.com/dodopayments/dodopayments-cli.git
cd dodopayments-cli
go install ./cmd/dodopayments
```

## クイックスタート

APIキーを設定します:

```bash
export DODO_PAYMENTS_API_KEY=your_api_key_here
```

基本的なコマンド構造:

```bash
dodopayments [resource] [command] [flags]
```

チェックアウトセッションを作成します:

```bash
dodopayments checkout-sessions create \
  --product-cart.product_id prod_123 \
  --product-cart.quantity 1 \
  --return-url https://yourdomain.com/return
```

## グローバルフラグ

| フラグ | 説明 |
|------|-------------|
| `--format` | 出力形式: `auto`, `json`, `yaml`, `pretty`, `explore` |
| `--transform` | gjson構文を使用して応答をフィルタリング |
| `--debug` | 詳細なログを有効にする |
| `--version`, `-v` | CLIバージョンを表示 |

## 一般的なコマンド

### 支払い

```bash
# List payments
dodopayments payments list --limit 20 --format json

# Retrieve payment
dodopayments payments retrieve pay_123

# Get line items
dodopayments payments line-items pay_123
```

### 顧客

```bash
# Create customer
dodopayments customers create \
  --email customer@example.com \
  --name "John Doe"

# List customers
dodopayments customers list

# Update customer
dodopayments customers update cus_123 --name "Jane Doe"
```

### サブスクリプション

```bash
# Create subscription
dodopayments subscriptions create \
  --customer-id cus_123 \
  --product-id prod_456

# List subscriptions
dodopayments subscriptions list

# Get usage history
dodopayments subscriptions usage-history sub_123
```

### 商品

```bash
# Create product
dodopayments products create \
  --name "Premium Plan" \
  --price.amount 2999 \
  --price.currency USD

# List products
dodopayments products list
```

## 使用量ベースの請求

### イベントの取り込み

```bash
dodopayments usage-events ingest \
  --events.event_id api_call_12345 \
  --events.customer_id cus_abc123 \
  --events.event_name api_request \
  --events.timestamp 2024-01-15T10:30:00Z \
  --events.metadata.endpoint /api/v1/users
```

### メーターの管理

```bash
# Create meter
dodopayments meters create \
  --name "API Requests" \
  --event-name api_request \
  --aggregation count

# List meters
dodopayments meters list
```

## 出力形式

### JSON (スクリプト用)

```bash
dodopayments payments list --format json | jq '.data[] | {id, amount}'
```

### プレティ (読みやすい形式)

```bash
dodopayments payments retrieve pay_123 --format pretty
```

### インタラクティブエクスプローラー

```bash
dodopayments payments list --format explore
```

### gjsonで変換

```bash
# Extract all payment IDs
dodopayments payments list --transform "data.#.id"

# Filter and extract
dodopayments payments list --transform "data.#(amount>5000)#.id"
```

<Tip>
`--format json`を`jq`と共に使用して、シェルスクリプトで強力なデータ処理を行ってください。
</Tip>

## スクリプト例

```bash
#!/bin/bash

# Export all payments to CSV
dodopayments payments list --format json | \
  jq -r '.data[] | [.id, .amount, .currency, .status] | @csv' > payments.csv

# Create customers from file
while IFS=',' read -r email name; do
  dodopayments customers create \
    --email "$email" \
    --name "$name" \
    --format json
done < customers.csv
```

## シェル補完

自動補完を有効にする:

```bash
# Bash
eval "$(dodopayments completion bash)"

# Zsh
eval "$(dodopayments completion zsh)"

# Fish
dodopayments completion fish | source
```

## 利用可能なリソース

CLIは24のリソースカテゴリに対するコマンドを提供します:

- `checkout-sessions` - チェックアウトセッション管理
- `payments` - 支払い操作
- `subscriptions` - 定期請求
- `customers` - 顧客アカウント
- `products` - 商品カタログ
- `licenses` - ソフトウェアライセンス
- `refunds` - 支払いの返金
- `disputes` - 支払いの紛争
- `webhooks` - イベントWebhook
- `meters` - 使用量メーター
- `usage-events` - 使用量追跡
- `discounts` - 割引コード
- `payouts` - 売上の支払い
- `brands` - マルチブランド管理
- さらに10のリソース

<Info>
`dodopayments --help`を使用して、すべての利用可能なリソースを確認し、`dodopayments [resource] --help`でリソース固有のコマンドを確認してください。
</Info>

## リソース

<CardGroup cols={2}>
<Card title="GitHubリポジトリ" icon="github" href="https://github.com/dodopayments/dodopayments-cli">
  ソースコードとリリースを表示
</Card>

<Card title="APIリファレンス" icon="book" href="/api-reference/introduction">
  完全なAPIドキュメント
</Card>
</CardGroup>

## サポート

CLIに関して助けが必要ですか？

- **Discord**: [コミュニティサーバー](https://discord.gg/bYqAp4ayYh)に参加
- **Email**: support@dodopayments.comまでご連絡ください
- **GitHub**: [リポジトリ](https://github.com/dodopayments/dodopayments-cli)に問題をオープン
